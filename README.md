# OpenResty VOMS container

This is a VOMS-enabled OpenResty VOMS container, based on the [ngx-http-voms
module][ngx-voms].

The image runs nginx as the `nginx` user.

[ngx-voms]: https://baltig.infn.it/storm2/ngx_http_voms_module
